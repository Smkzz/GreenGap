[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "greengap"
dynamic = ["version"]
description = "Find what your green CI never ran."
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [{ name = "GreenGap contributors" }]
keywords = ["ci", "github-actions", "pytest", "testing"]
urls = { Homepage = "https://github.com/Smkzz/GreenGap", Repository = "https://github.com/Smkzz/GreenGap", Issues = "https://github.com/Smkzz/GreenGap/issues", Changelog = "https://github.com/Smkzz/GreenGap/blob/main/CHANGELOG.md" }
dependencies = ["PyYAML>=6.0"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Software Development :: Testing",
  "Typing :: Typed",
]

[project.scripts]
greengap = "greengap.cli:main"

[project.optional-dependencies]
dev = [
  "build>=1.2",
  "mypy>=1.10",
  "pytest>=9.0.3",
  "ruff>=0.5",
  "types-PyYAML>=6.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.dynamic]
version = { attr = "greengap.__version__" }

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
python_functions = ["test_*"]
python_classes = ["Test*"]
addopts = "-ra"

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.11"
packages = ["greengap"]
strict = true
warn_unused_configs = true
exclude = ["^tests/"]

[tool.coverage.run]
branch = true
source = ["greengap"]
